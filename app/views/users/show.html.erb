<div class="show-user-main">
  <div class="show-user-left">
    <div class="user-date">
      <% if current_user.image.present? %>
        <%= image_tag current_user.image_url, class: "show-user-image" %><br>
      <% else %>
        <%= image_tag "icon.png", class: "show-user-image" %><br>
      <% end %>
      ニックネーム：<%= current_user.nickname %><br>
      匿名時のID：<%= current_user.public_uid %><br>
    </div>
    <div class="user-topics-date">
      投稿したトピック数：<%= current_user.topics.count %>件<br>
      コメントした数：<%= current_user.comments.count %>回<br>
      Goodボタンを押した数：<%= current_user.likes.count %>回<br>
      Badボタンを押した数： <%= current_user.bads.count %>回<br>
    </div>
  </div>
  <div class="show-user-right">
    <h1><%= current_user.nickname %>さんの投稿記事一覧</h1>
    <% if @topics.present? %>
      <% @topics.each do |topics| %>
        <div class="user-topics">
        <%= time_ago_in_words(topics.created_at) %>前
          <%= link_to topics.title, topic_path(topics), class:"user-topic" %>
          　<i class="far fa-comment"></i><%= topics.comments.count %>件
        </div>
      <% end %>
    <% else %>
      投稿したトピックはまだありません。
    <% end %>
  </div>
</div>